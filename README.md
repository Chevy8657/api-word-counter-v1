# Word Counter (v1)

## Description
A stateless utility API that returns the integer count of words in a string.

## Endpoint
POST `/v1/word-count`

## Payload
```json
{ "input": "Hello world from the factory" }

